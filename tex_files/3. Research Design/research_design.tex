% =====================================================================
%  Research Design
%  ---------------------------------------------------------------------
%  This file is meant to be \input from your main document, e.g.:
%      \input{research-design}
%
%  Required packages (load these in your MAIN preamble):
%      \usepackage{amsmath}    % aligned equations, \text, \models, ...
%      \usepackage{graphicx}   % \includegraphics for the figures
%      \usepackage{booktabs}   % \toprule / \midrule / \bottomrule
%      \usepackage{tabularx}   % width-fitting validation table (pulls in array)
%
%  Revision notes (July 2026 pass, items 4-7):
%   * Repeatability gaps are marked inline with \textit{[FILL: ...]} --
%     grep for "FILL" to find every slot. Each is listed in the summary
%     accompanying this file.
%   * Appendix plan: Appendix A holds the full result tables and the
%     keyword bar charts (kept there permanently by author decision, as
%     the in-body figures present the same information more digestibly;
%     consider retitling from "Deferred..." to "Supplementary Figures
%     and Tables" since nothing is deferred any more);
%     Appendix B = worked example (query -> IR -> Cypher), stub compiles
%     as "[In preparation]" -- ensure \label{app:worked} sits on the stub;
%     Appendix C = prompts and tool schemas (keyword generation prompt,
%     IR-extraction system prompt + forced tool schema), ALREADY PRESENT
%     in the compiled document -- ensure \label{app:prompts} sits on it;
%     Appendix D = error exemplars (one hallucinated keyphrase with its
%     source text; one filter_distractor failure with query + produced
%     IR), being added by the author (item 14).
%     Body references to B and C use \ref{app:worked}/\ref{app:prompts},
%     so those labels must exist in main.tex or they compile as "??".
%   * The old Appendix~A reference for node properties now points at the
%     schema figure, which carries the property set in-figure.
%   * Section III-C4 now documents stages 2 and 3 rather than pointing
%     elsewhere; Section III-B's stage-1 description now matches the
%     entity types III-C3 and III-D2 actually use (not keywords only).
%   * fig:pipeline is kept as-is per author; redraw TODO noted at site.
%   * Stale header notes about the memory-budget placeholder table and
%     the "[to be added]" candidate list removed (both resolved).
%   * July fill pass: snapshot slot narrowed to a checked shortlist;
%     keyword decoding params filled (temp 0, 400-token cap, no reasoning
%     effort, API-default sampling); confidence threshold and candidate
%     caps removed per author; similarity = Memgraph's built-in cosine
%     over the candidate pool; rerank ties broken by citation count;
%     8 GB GPU evaluation environment noted in III-D. Quantisation
%     wording is precision-neutral: the author's "fp16 to int8" referred
%     to benchmark model WEIGHTS (now covered by the III-D environment
%     note); the stored vectors' source precision is unconfirmed (see
%     VERIFY in Ingestion and Storage). OPEN (grep PENDING/VERIFY): the
%     unattributed "10,000"; per-work keyphrase bound; stored-vector
%     source precision; exact Memgraph similarity procedure.
%   * Second fill pass (author answers, this session): snapshot date set to
%     2026-02-03 (not load-bearing per author); keyword extraction capped at
%     FIVE keyphrases per work (corrects an earlier "uncapped" answer; the
%     "10,000" was a removed compiler limit, not this cap); benchmark weight
%     quantisation specified as post-training weight-only int8 in the III-D
%     environment note, decoupling it from the stored vectors; dense scoring
%     named as Memgraph MAGE node_similarity cosine (int8 handling left to the
%     module); reranker input truncation stated as model-default (author
%     unsure --- flagged). Rescued two sentences a prior pass had accidentally
%     commented out: the embedding-module description (III-B) and the
%     "top-100 retained" line (III-C4). The six III-D6 system-evaluation FILL
%     slots are LEFT as pending design decisions per author. Still OPEN (grep
%     VERIFY): field list vs pipeline; schema-figure property set; VM RAM
%     figure; GPU card; 116-query sum; single-author annotation; exact
%     Memgraph procedure name; reranker max length; (data_and_results) the
%     "reasoning on" effort level.
%   * Verification pass (separate session, July 2026): (i) snapshot date
%     REVERTED to a FILL slot -- 2026-02-03 is a real standard-format
%     release (checked against OpenAlex's published RELEASE_NOTES) and
%     remains the likely candidate, but which release this ingest consumes
%     is an inference, and the ingest had not run when the date was written;
%     (ii) removed "instructed to return only spans that occur in the
%     source" -- the prompt reproduced in Appendix C carries no such
%     constraint (knock-on for Section V-A1 noted at site); (iii) relaxed
%     the blanket int8 weight-quantisation claim in III-D, which
%     contradicted the Q4 annotations on the local IR-extraction models in
%     Table XIV; (iv) harmonised the stage-2 retention line to "up to the
%     top 100" per III-B; (v) corrected the appendix status notes above.
%     NEW VERIFY flags: five-per-work keyphrase cap vs Appendix C's
%     max_keywords n=20 parse cap; "exact" flag absent from the Appendix C
%     benchmark tool schema. All other content checked against the compiled
%     draft and found faithful; the six III-D6 system-eval FILLs remain
%     deliberately open.
% =====================================================================

\section{Research Design}
\label{sec:research-design}

\subsection{Design Approach}
\label{subsec:design-approach}

This section sets out the methodology for designing and evaluating the system in order to answer the project's research questions. Accordingly, the research design was required to do two things: firstly, document a retrieval architecture in enough detail to be reproduced; secondly, define an evaluation methodology that establishes whether the architecture works and isolates the contribution of each component.

The following approach was used for systematically designing and validating the system:

\begin{enumerate}
    \item High-level design of the architecture, the graph schema, and the ingestion mechanism;
    \item Detailed design of each retrieval module;
    \item Isolated validation of each module against an appropriate benchmark;
    \item Validation of the integrated system against the research questions.
\end{enumerate}

This ensures that each design decision made in the earlier stages has a corresponding benchmark in the later stages. The earlier stages document the design for reproducibility, and the later stages test it for validity. This correspondence is what makes the methodology suitable for this thesis.

Validating components in isolation also addresses a specific risk in this pipeline. Because each query passes through several components in sequence, an error observed at the output could originate in any one of them, and an end-to-end test alone cannot identify which. Validating each component separately removes that ambiguity, measuring the contribution of each rather than confounding them.

\subsection{System Architecture}
\label{subsec:system-architecture}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{images/system_architecture.png}
    \caption{System architecture. An ingestion pipeline builds the knowledge graph that a three-stage retrieval cascade queries. The candidate pool size is illustrative and varies by query.}
    \label{fig:system-architecture}
\end{figure}

The system has two parts. An offline ingestion pipeline builds the knowledge graph from source data, and an online retrieval pipeline answers queries against it in three stages. Figure~\ref{fig:system-architecture} shows the overall design.

At a high level, the system uses an ingestion pipeline that:

\begin{itemize}
    \item takes OpenAlex as the source of paper data, namely the title, abstract, publication date, authors and their institutional affiliations, and the venue and publisher of each work; % VERIFY: confirm this field list matches what the pipeline actually extracts (the schema's Person, Institution, Venue and Publisher nodes imply all of these).
    \item extracts the keyphrases and dense embedding of each work from its title and abstract;
    \item transforms the data into the ontology chosen to represent the scholarly domain;
    \item loads the result into the database.
\end{itemize}

OpenAlex was chosen as the source because it is the largest fully open scholarly index and is the successor to the Microsoft Academic Graph, as established in Section~\ref{subsec:kg-infrastructure}. The keyphrases and embeddings extracted in this stage are not incidental products. They are the data that the first and second retrieval stages depend on. How each field is sourced and extracted, and the schema the data is transformed into, are documented in Section~\ref{subsec:component-design}.

For retrieval, the system uses a three-stage pipeline:

\begin{itemize}
    \item Structured graph retrieval, which produces the initial candidate pool. A natural-language query is translated into a structured representation of the typed entities to search for, such as keywords, researcher names, institutions, and venues, and the Boolean logic that combines them, with entities taken either from the query or from explicit user input. Non-exact entities are matched against the graph's corresponding nodes with BM25 over text indexes, exact entities by direct property match, and the works connected to the matched entities are retrieved and combined under the requested AND, OR, and NOT logic.
    \item Dense embedding retrieval, which narrows the candidate pool by semantic similarity. The query and each candidate paper are represented as high-dimensional vectors, and the candidates closest to the query by cosine similarity are kept, up to the top 100.
    \item Cross-encoder re-ranking, which orders the final results by relevance. A cross-encoder takes the query and a paper's title and abstract together and returns a single relevance score, and the top 100 from the second stage are re-ranked by that score.
\end{itemize}

This extends the two-stage retrieve-then-rerank pattern common to large-scale retrieval systems, placing a knowledge-graph candidate selector in front of the embedding-retrieval and cross-encoder re-ranking stages. Because the three stages increase in both accuracy and cost, each runs over a smaller set than the last, which is necessary because the most accurate stage cannot be run over the full corpus.

Each stage also performs a function the others cannot, which is why all three are present rather than any one alone. The first stage lets the system express typed or structural queries that the other two cannot. As established in Section~\ref{sec:lr-dense}, single-vector dense retrieval cannot represent requests such as papers from a particular author on a given topic published after a given year. Structured retrieval expresses these directly and returns a high-recall candidate set for the later stages to rank. Its output is unbounded. The structured representation and its compilation into a graph query are documented in Section~\ref{subsec:component-design}.

The second stage ranks that candidate set by semantic similarity, which structured retrieval cannot do, since it matches only on the entities and keywords a work shares with the query. Dense embeddings rank candidates by their semantic relatedness to the full query, capturing relevance that exact-term matching misses. Section~\ref{sec:lr-dense} also showed that dense retrieval used alone misses a substantial share of relevant papers and is weak on entity-heavy queries, which is why it ranks a structured candidate set here rather than serving as the primary retrieval mechanism. The embeddings are quantised to reduce their memory footprint, with the quantisation and its justification documented in Section~\ref{subsec:component-design}.

The third stage refines the ranking once more. A bi-encoder compares two vectors that were computed separately, whereas a cross-encoder reads the query and the document together and can weigh their interaction directly, which Section~\ref{sec:lr-dense} identified as the source of the accuracy that late-interaction and cross-encoder approaches achieve over single-vector retrieval. This makes it more accurate but too expensive to run over many papers, which is why it is placed last and applied only to the 100 candidates the earlier stages have selected.

\subsection{Component Design}
\label{subsec:component-design}

\subsubsection{Knowledge Graph Schema}
\label{subsubsec:kg-schema}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{images/Schema.png}
    \caption{Scholarly knowledge graph schema, with the properties held on each node type.} % VERIFY: caption assumes the updated figure carries the per-node property set moved in from the old Appendix A.
    \label{fig:kg-schema}
\end{figure}

The schema is general purpose, built from generic entities such as works, keywords, and authors, so that it generalises beyond any single domain despite the biomedical focus of the data it is deployed on.

The design was derived from the knowledge graphs surveyed in Section~\ref{subsec:kg-infrastructure}, which commonly share a similar sub-ontology over the six node types required for retrieval (Work, Person, Institution, Venue, Publisher, and Keyword) and their associated edges. This schema is based on that common structure.

The main point of departure concerns keyword modelling. SemOpenAlex, which is also built on OpenAlex, attaches keywords to the topics assigned to works rather than to the works directly, because OpenAlex derives its keywords at the topic level. Since this schema derives its own keywords per work (Section~\ref{subsubsec:ingestion-storage}), it attaches them directly to the work rather than through a topic node. The resulting schema, together with the properties held on each node, is shown in Figure~\ref{fig:kg-schema}; the property set was refined through internal testing and reduced to the current form.

\subsubsection{Ingestion and Storage}
\label{subsubsec:ingestion-storage}

For all purposes excluding benchmarks, the complete OpenAlex snapshot of \textit{[FILL: release date --- likely the 2026-02-03 release; confirm from RELEASE\_NOTES.txt at the snapshot root]} was used as the data source, with the version pinned in line with the snapshot-specific citation practice argued for in Section~\ref{subsec:kg-infrastructure}. As established there, OpenAlex is the most suitable index due to its large corpus of works and pre-processed metadata.
% REVERTED to FILL (verification pass): 2026-02-03 is a real standard-format
% release (verified against OpenAlex's published RELEASE_NOTES), but which
% release this ingest consumes was inferred from the 23 Mar--1 Apr download
% window, and the full ingest had not yet run when the date was written. This
% sentence exists to argue snapshot-specific citation, so the body must not
% assert an unconfirmed version. To resolve in ~30 s once the ingest box is at
% hand: `head RELEASE_NOTES.txt` at the snapshot root. Note OpenAlex's 2026
% layout also exposes dated full builds (e.g. full/<YYYY-MM-DD>/) alongside
% the quarterly public releases; if the download came from that channel, cite
% the dated folder instead.

A biomedical subset was then selected from this snapshot by filtering on the OpenAlex field tag and retaining eleven fields, namely Medicine; Agricultural and Biological Sciences; Biochemistry, Genetics and Molecular Biology; Psychology; Health Profession; Chemistry; Neuroscience; Immunology and Microbiology; Nursing; Pharmacology, Toxicology and Pharmaceutics; and Dentistry. The subset comprises approximately 85 million works, gives the deployed system its biomedical focus, and leaves the schema itself general purpose.

For each work in the subset, a keyword generation module and an embedding generation module produce the per-work keywords and the dense embedding that the structured and dense retrieval stages depend on. The keyword module prompts a language model to extract the most relevant keyphrases from the title and abstract; generation runs at temperature zero with an output cap of 400 completion tokens, no reasoning effort requested, and all other sampling parameters at their API defaults, with at most five keyphrases retained per work; the prompt is reproduced in Appendix~\ref{app:prompts}. Temperature zero does not make the hosted endpoint deterministic, which is why the module's run-to-run stability is measured separately (Section~\ref{subsec:evaluation-validation}).
% RESOLVED (keyphrase cap): author correction --- the keyword extractor is
% capped at FIVE keyphrases per work (consistent with the F1@5 keyword metric
% in III-D). The earlier "uncapped" answer conflated this slot with the search
% component. The stray "10,000" is unrelated to this cap --- see the note under
% Structured Graph Retrieval.
% CHANGED (verification pass): dropped "instructed to return only spans that
% occur in the source". The prompt reproduced verbatim in Appendix C carries
% no such constraint --- it frames the task as extraction ("Extract the most
% relevant keywords from the provided scientific text") but never forbids
% novel spans. A marker comparing body to appendix would catch the mismatch.
% Restore the claim only if a stricter deployed prompt variant actually
% carries it, and then reproduce THAT variant in the appendix. Knock-on:
% Section V-A1's "despite prompting to suppress this" needs the same
% softening, e.g. "despite the extraction framing of the prompt".
% VERIFY (added, verification pass): Appendix C's reproduced benchmark config
% documents max_keywords (n) = 20 as its parse cap. Under F1@5 the first five
% keyphrases decide the score either way, so the results likely stand, but
% reconcile the two numbers in text (production retention cap of five vs.
% benchmark parse cap of twenty) so this paragraph and the appendix do not
% read as contradictory.
The embedding module encodes the concatenated title and abstract into a single dense vector. A number of models for each module were benchmarked to inform the selection, as outlined in Section~\ref{subsec:evaluation-validation}, and the selected models are reported with those results.

The resulting data was transformed to the structure of the ontology and loaded into Memgraph using its documented bulk-loading practices, with the full property-level mapping from OpenAlex fields to graph elements maintained as a YAML specification alongside the codebase. Memgraph was used as an in-memory graph database, on an Azure-hosted virtual machine provisioned with sufficient memory to hold the full graph (over 500\,GB of RAM). % VERIFY: confirm the current VM memory figure.
Memgraph was chosen for the query performance of an in-memory store at this scale, for its MAGE library of graph algorithms, and for a licence compatible with the intended use. Because the graph resides in memory, the dense embeddings are quantised to 8-bit integers to fit within the available memory (see Table~\ref{tab:bienc-deploy} for the resulting footprint).
% RESOLVED (scheme decoupled): author confirms the "fp16 to int8" referred to
% WEIGHT quantisation of oversized benchmark models (post-training, weight-only:
% weights cast to int8 at load, no calibration/QAT), now stated in the III-D
% evaluation-environment note --- distinct from these STORED vectors. The stored
% vectors' exact source dtype before the int8 cast is a minor implementation
% detail left to the ingestion code; at query time the int8 values are consumed
% directly by Memgraph's cosine procedure (III-C4).

\subsubsection{Structured Graph Retrieval}
\label{subsubsec:structured-retrieval}

The first retrieval stage turns a natural-language query into a Memgraph-compatible Cypher query through a sequence of learned and deterministic sub-modules.

The query is first normalised, which collapses whitespace, resolves year hints into year bounds, and pulls out quoted phrases as exact-match terms, preserving case so that proper nouns and acronyms survive.

The normalised query is then converted into an Intermediate Representation (IR) using an LLM via a forced tool call whose schema mirrors the representation; the system prompt and tool schema are reproduced in Appendix~\ref{app:prompts}. The IR couples two components: firstly, a flat list of typed entities, each carrying a type, value, confidence and exact flag; secondly, a recursive boolean expression over those entities using AND, OR and NOT. This design aims to decouple LLM-based intent extraction from deterministic query construction, taking the flexibility of the former whilst keeping the correctness guarantee of the latter, as noted in Section~\ref{sec:lr-nl2q}. The representation is then validated, rejecting any query with no entities to anchor the traversal.
% NOTE: an earlier confidence-based entity drop was removed from the system
% (author, July 2026). The IR field list above still includes confidence per
% the schema description; reconcile with the Appendix C tool schema (either
% the field remains and is unused for filtering, or remove it from the field
% list here).
% ALSO (verification pass): the tool schema reproduced in Appendix C carries
% type/value/confidence only --- no "exact" flag --- while this paragraph
% (and the compiler's exact-vs-BM25 split below) says the production IR has
% one. The exact flag is almost certainly real in production (the normaliser's
% quoted-phrase extraction needs somewhere to land), so the fix is on the
% appendix side: reproduce the production schema, or keep the trimmed
% benchmark schema and annotate the trim explicitly so body and appendix do
% not read as contradictory.

The deterministic compiler turns the validated representation into Cypher. Rather than translating the boolean tree into nested graph patterns, it runs one subquery per entity, matching non-exact entities with BM25 over a per-entity-type text index (Memgraph's text search module) and exact entities by direct property match. It then keeps each work whose matched entities satisfy the boolean expression.
% RESOLVED: author confirms no candidate caps in the current compiler (the
% May worked example's 10000/1000 limits are gone). The Appendix B worked
% example must therefore match the current compiler's output. The stray
% "10,000" was one of these removed compiler limits, not the keyphrase cap
% (which is five per work --- see the note under Ingestion and Storage).

Formally, for each entity $e_{i}$, let $W_{i}$ be the set of works that match it, subject to the work-level filters. Satisfaction of an expression $\varphi$ by a work $w$, written $w \models \varphi$, is defined by recursion over the structure of $\varphi$,
%
\begin{align*}
    w \models e_{i} &\quad\text{iff}\quad w \in W_{i}, \\
    w \models \mathrm{AND}(\varphi_{1}, \ldots, \varphi_{k}) &\quad\text{iff}\quad w \models \varphi_{j} \text{ for every } j, \\
    w \models \mathrm{OR}(\varphi_{1}, \ldots, \varphi_{k}) &\quad\text{iff}\quad w \models \varphi_{j} \text{ for some } j, \\
    w \models \mathrm{NOT}(\varphi) &\quad\text{iff}\quad \lnot\,(w \models \varphi).
\end{align*}
%
The leaves $e_{i}$ are the query's entities and $E$ is its full boolean expression, so the candidate set returned by the stage is
%
\[
    \{\, w \in \bigcup_{i} W_{i} \mid w \models E \,\},
\]
%
where $\bigcup_{i} W_{i}$ is the set of works matching at least one entity. Restricting the universe in this way ensures that a NOT excludes works within the matched set rather than admitting the works that match nothing, and the recursion preserves arbitrary nesting of the operators.

The compiled query runs against the database and returns the candidate pool together with its total match count. A worked example of a query, its intermediate representation, and the compiled Cypher is given in Appendix~\ref{app:worked}.
% PLACEHOLDER (appendix B): reference left as literal text until the worked
% example appendix is added and wired into main.tex.

\subsubsection{Dense Embedding Retrieval and Re-ranking}
\label{subsubsec:dense-rerank}

The second and third stages are established techniques rather than novel contributions of this work, and the reasoning for their place in the cascade is given in Section~\ref{subsec:system-architecture}; the retrieve-then-rerank pattern they follow is reviewed in Section~\ref{sec:lr-dense}. Their configuration in this system is as follows.

In the dense retrieval stage, the query is embedded at query time with the selected bi-encoder, and each candidate work carries the embedding precomputed for it at ingestion (Section~\ref{subsubsec:ingestion-storage}), stored at int8 in the graph. Similarity is cosine, computed over the candidate pool rather than through an approximate index, with the computation performed by the cosine-similarity procedure of Memgraph's MAGE \texttt{node\_similarity} module over the stored int8 vectors; the handling of the 8-bit values is internal to that module.
% RESOLVED (per author): scoring uses Memgraph's MAGE node_similarity cosine
% procedure; int8 handling is internal to that module (left as a database
% implementation detail). MINOR: confirm the exact call against the query code
% (node_similarity.cosine vs cosine_pairwise, or native
% vector_search.cosine_similarity) --- all are exact cosine over the pool.
The candidates most similar to the query are retained, up to the top 100.

In the re-ranking stage, each retained candidate is paired with the query as a (query, title and abstract) input to the selected cross-encoder, and the single relevance score returned per pair determines the final ordering, with ties broken by citation count. No custom truncation is applied to the paired input; each pair is passed to the model as-is, subject to the cross-encoder's own maximum-sequence-length handling.
% RESOLVED (default, per author): no explicit truncation policy was applied ---
% inputs are passed as-is, so any truncation is the model/tokeniser default (its
% max sequence length). ASSUMPTION: stated as default behaviour because the
% author was unsure; if a specific max length or truncation side matters for
% reproducibility, confirm the tokeniser setting and note it here.

Both models are selected by the benchmarking of Section~\ref{subsec:evaluation-validation} and reported with those results, and the generation and quantisation of the stored embeddings, together with the memory budget, are covered in Section~\ref{subsubsec:ingestion-storage}.

\subsection{Evaluation and Validation Design}
\label{subsec:evaluation-validation}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{images/retrieval_pipeline.png}
    \caption{End-to-end representation of the pipeline, from natural-language query to ranked results.}
    \label{fig:pipeline}
\end{figure}
% TODO (item 7, deferred by author): fig:pipeline to be redrawn to publication
% standard; kept as-is for this pass.

\begin{table}[htbp]
    \centering
    \caption{Validation design}
    \label{tab:validation-design}
    \begin{tabularx}{\linewidth}{@{}p{2.4cm}%
        >{\raggedright\arraybackslash}X%
        >{\raggedright\arraybackslash}X%
        >{\raggedright\arraybackslash}X@{}}
        \toprule
        \textbf{Component} & \textbf{Validation Target} & \textbf{Dataset} & \textbf{Metric} \\
        \midrule
        Keyword extraction   & Keyword quality              & KPBiomed, OAGKX, PubMedAKE     & Present/absent F1@5 (stemmed)        \\
        IR extraction        & Query-understanding accuracy & Hand-built stratified gold set & Entity match + logical equivalence   \\
        Dense bi-encoder     & Retrieval quality            & SciRepEval ad-hoc search       & nDCG                                 \\
        Cross-encoder        & Re-ranking quality           & SciRepEval ad-hoc search       & nDCG                                 \\
        Embedding quantisation & Int8 accuracy cost         & SciRepEval ad-hoc search       & nDCG (full precision vs.\ int8)      \\
        Assembled cascade    & End-to-end retrieval behaviour (RQ3) & Curated realistic query set (\textit{[FILL: $n$]} queries) & Per-query characterisation + light relevance judgments \\
        \bottomrule
    \end{tabularx}
\end{table}

Each component designed in Section~\ref{subsec:component-design} is validated independently, against established benchmarks to allow for a comparison of models, from which one can be selected for the final system. This component-wise validation strategy, as summarised in Table~\ref{tab:validation-design}, acts as the counterpart to the design outlined in Section~\ref{subsec:component-design}, following the design and validation approach of Section~\ref{subsec:design-approach}. The table's final row is the system-level evaluation of Section~\ref{subsubsec:system-level}, which validates the composed cascade rather than a component. The outcome of each benchmark, including the model selected, is reported in the Data and Results section. Self-hosted candidates were evaluated on a single 8\,GB GPU, and models exceeding its memory at their native precision were run with quantised weights (post-training, weight-only, with no quantisation-aware training), with the precision used annotated per model in the results tables.
% CHANGED (verification pass): the previous blanket claim ("int8 ... cast to
% 8-bit integers at load time, with no calibration") contradicted the results
% tables --- the local IR-extraction models are annotated "Local (Q4)" in
% Table XIV, i.e. 4-bit GGUF weights quantised offline, while the int8
% cast-at-load description fits the oversized embedder/reranker candidates
% (Qwen3-*-4B at int8). "No calibration" also does not hold uniformly across
% GGUF quant types. The per-model annotation is the accurate common
% denominator; if the int8/load-time detail is worth stating, scope it to the
% specific models in the results-table notes rather than here.
% VERIFY: GPU spec (author: "8gb"); confirm the card if worth naming.

\subsubsection{Keyword Generation}
\label{subsubsec:keyword-generation}

The keyword generation module of Section~\ref{subsubsec:ingestion-storage} is evaluated against three keyphrase datasets, namely KPBiomed \cite{houbre2022kpbiomed}, OAGKX \cite{cano2020oagkx}, and PubMedAKE \cite{sheng2022pubmedake}, each of which pairs documents with reference keyphrases. Reference keyphrases are split into present (extractive) keyphrases, which occur verbatim in the source text, and absent (abstractive) keyphrases, which do not and so must be generated. Each is scored by F1@5, the harmonic mean of precision and recall over the top five predicted keyphrases, with light stemming, so that a keyphrase and its stem count as a match while a different word of the same meaning does not. The present/absent split isolates faithful extraction from generation, which matters because a language-model extractor can hallucinate keyphrases the source does not contain.

The candidates place a large-language-model generator, GPT-5.4-nano, against established statistical and neural baselines: TF-IDF \cite{salton1988termweighting}, YAKE \cite{campos2020yake}, TextRank \cite{mihalcea2004textrank}, and MultipartiteRank \cite{boudin2018multipartiterank} as statistical or graph-based extractors; KeyBERT-MiniLM \cite{grootendorst2020keybert} and KBIR-inspec \cite{kulkarni2022kbir} as neural extractors; and KeyBART \cite{kulkarni2022kbir} as a fine-tuned generative model. The purely extractive baselines can match only present keyphrases by construction, whereas the generative models can also attempt absent ones, so the present/absent split doubles as a comparison between the two families. The LLM generator is sampled at temperature zero, which does not make the hosted endpoint deterministic, so its stability is additionally measured over repeated runs; the baselines decode deterministically (greedy decoding for the generative models, fixed score-based tie-breaking for the statistical extractors) and are reproducible by construction.

\subsubsection{IR Extraction}
\label{subsubsec:ir-extraction}

This benchmark measures how well the model turns an unstructured query into the structured nested boolean representation, the IR (Section~\ref{subsubsec:structured-retrieval}). The model's output is compared against a correct IR written by hand, an approach known as intrinsic evaluation, rather than judging the model by the quality of the final search results. This is done to separate the two possible sources of error. If the system were judged only on its final results, a poor result could come from a mistake in the translation step or in the later search stages, with no way to tell which. Comparing against a correct IR measures the translation step on its own.

The benchmark was built by hand, because the literature (Section~\ref{sec:lr-gap}) found that no existing benchmark covers this task in the scholarly domain. It comprises 116 queries stratified into ten structural types (Table~\ref{tab:ir-stratum}), % VERIFY: 116 is the sum of the per-stratum counts in tab:ir-stratum; confirm against the gold set.
and was constructed as follows:

\begin{itemize}
    \item A set of sample queries was curated to match the vocabulary expected of users, including technical keywords, researcher names, institutions, and venues, so that every entity type the system supports is exercised. The queries span a range of difficulty, from a single topic, through queries that combine topics with AND, OR, and NOT, up to queries that group these with brackets. The simple queries were taken from an existing retrieval dataset, SciRepEval, and the harder ones, which everyday queries rarely contain, were based on the structure of expert queries written for systematic reviews, the exhaustive evidence searches common in fields like medicine.
    \item For each query, the correct nested boolean representation was written by hand by the author. % VERIFY: confirm single-author annotation; this is also flagged as a limitation for Section V (item 15).
    Where a query was ambiguous in how its terms should be grouped, the wording was tightened or brackets were added, so that each query has a single intended answer.
\end{itemize}

Each model is then scored against the hand-written answers on two parts. The first is the entities, the individual terms the model pulled out. These are compared to the correct terms allowing small surface differences, such as a word and its stem, but not accepting a different word of the same meaning, since this stage is meant to capture the terms the user actually wrote rather than paraphrase them. The second is the expression, the way those terms are combined with AND, OR, and NOT. This is judged by logical equivalence, meaning the answer counts as correct if it selects the same papers as the hand-written one, even when written in a different but equivalent form. The same request can be arranged in more than one valid way, for example A AND B is the same as B AND A. Several candidate models are scored this way, with the selected model and the scores reported in the Data and Results section.

\subsubsection{Dense Retrieval}
\label{subsubsec:dense-retrieval}

The dense bi-encoder is evaluated on the ad-hoc search tasks of SciRepEval, namely NFCorpus, TREC-CoVID, and Search, measured by nDCG. SciRepEval is an established benchmark for scientific document retrieval (Section~\ref{sec:lr-bench}), and nDCG, which scores how highly the relevant papers sit within the ranking, is the standard measure of ranking quality and keeps the stage comparable to prior work.

The candidates comprise general-purpose embedders (Qwen3-Embedding at 0.6B and 4B, EmbeddingGemma-300M, mxbai-embed-large-v1, BGE-M3, and the hosted text-embedding-3-small) alongside the scientific fine-tuned SPECTER2 in both its base and ad-hoc-query variants. The set contrasts general-purpose against domain-tuned encoders, and self-hostable against paid-API models (text-embedding-3-small being the sole hosted option), the two axes along which the results divide.

\subsubsection{Cross-Encoder Re-ranking}
\label{subsubsec:cross-encoder}

The cross-encoder is evaluated on the same SciRepEval ad-hoc search tasks under the same metric, by re-ranking the retrieved candidates, so the gain it adds can be read directly against the bi-encoder alone.

Thirteen rerankers are compared: commercial APIs (Cohere Rerank 4.0 in its Pro and Fast tiers); self-hostable neural rerankers (the Qwen3-Reranker at 0.6B and 4B, jina-reranker-v2, and mxbai-rerank-large-v1); a biomedical domain-specific model (MedCPT); and older cross-encoder baselines (the ms-marco-MiniLM and bge-reranker families, and gte-reranker-modernbert-base). This spread lets the results separate model family and recency from raw parameter count, and general-purpose rerankers from the domain-specific one.

\subsubsection{Embedding Quantisation}
\label{subsubsec:embedding-quantisation}

Quantising the embeddings to int8 (Section~\ref{subsubsec:ingestion-storage}) trades precision for memory, so its cost is measured rather than assumed. The bi-encoder evaluation above is repeated with the embeddings held at int8 and compared against the same embeddings at their unquantised precision, and the resulting difference in nDCG is the accuracy cost attributed to the quantisation. This ablation concerns the stored vectors, and is distinct from the weight quantisation used to fit oversized self-hosted candidates on the evaluation GPU, which affects the model rather than its output and is annotated per model in the results tables.

\subsubsection{System-Level Evaluation}
\label{subsubsec:system-level}

The composed system has no off-the-shelf end-to-end benchmark, which reflects the gap identified in Section~\ref{sec:lr-limitations}. Under the separation of concerns above, however, the system's behaviour follows from its parts. The structured stage returns the papers matching the boolean query, established by the IR and keyword evaluations together with the deterministic compiler, and the dense stages rank those papers, established by their benchmarks. The component results therefore stand as the primary evidence for the system, subject to two limits stated plainly. First, the dense models are benchmarked on a full corpus rather than on the candidate set they rank within the cascade, so their measured quality is a close proxy for their role here rather than a direct measurement of it. Second, the system returns only papers that match the boolean query, so a paper that is relevant but does not match is not retrieved, which is the precision-oriented behaviour the design intends rather than a defect.

A system-level evaluation confirms both points directly by comparing three systems over the same query set: the assembled cascade, a retrieval-augmented LLM (\textit{[FILL: the system and configuration used, e.g.\ model, retrieval source, settings]}), and Google Scholar. The query set comprises \textit{[FILL: $n$, suggest 20--30]} queries, selected to \textit{[FILL: selection criteria, e.g.\ span the structural strata of Section~\ref{subsubsec:ir-extraction} and reflect the biomedical workflows of the industry partner's users]}. For each query, the top \textit{[FILL: $k$, suggest 10--20]} results from each system are recorded and characterised qualitatively, what each system surfaces and what it misses, with light relevance judgments assigned by \textit{[FILL: who judges, e.g.\ the author, or the author plus an industry-partner researcher]} on a \textit{[FILL: relevance scale, e.g.\ binary or 0--2 graded]} scale. This comparison is the instrument for RQ3; its results appear in Section~\ref{sec:results-system} and its analysis in Section~\ref{subsec:rq3}.
% NOTE (user decision pending): the experiment has not yet been run, so the
% FILL slots above are design decisions still to be made, not just missing
% facts. The bracketed suggestions are starting points, not commitments.