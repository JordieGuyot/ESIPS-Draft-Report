% ============================================================================
%  Data and Results
%  Include from main.tex with:  \input{data_and_results}   (or \include{...})
%
%  Requires in the preamble of main.tex:
%      \usepackage{booktabs}
%      \usepackage{graphicx}
%      \usepackage{array}      % only if you later add >{...} column types
%
%  Figures use placeholder paths under figures/ -- replace with your exports.
%  Tables and figures are referenced with \ref so numbering stays dynamic and
%  any table can be moved to an appendix without breaking the prose.
%
%  Revision notes (July 2026 de-duplication pass):
%    - This section now REPORTS only; interpretation lives in Section V.
%      Moved to V: the reasoning-ablation ceiling/budget reading, the Search
%      saturation mechanism ("small pool leaves little room to reorder"), and
%      all causal/diagnostic language.
%    - Table notes for tab:ir-reasoning and tab:xenc-deploy trimmed to match
%      (takeaway clauses removed; facts retained).
%    - Prose describing tab:bienc-deploy and tab:xenc-deploy now matches the
%      columns those tables actually contain (the old text promised
%      "precision" and "self-hostable" columns that were never there).
%    - Setup sentences that duplicated Section III-D verbatim are trimmed to
%      pointers; only the definitions needed to read the tables remain.
%    - Item 11 remainder: the IR reasoning-ablation paragraph now reconciles
%      Table XIV's "default effort, 384 reasoning tok" for gpt-5-mini (= the GPT-5
%      family's medium default) with Table VIII's off/on conditions, which
%      bracket it. One VERIFY remains: the exact effort level of "on".
%
%  Revision notes (July 2026 quantisation-results pass):
%    - Embedding Quantisation placeholder (item 8) replaced with the results
%      of the dimension x precision matrix (design widened in III-D5 to
%      match). Two in-body tables added (tab:quant-te3s, tab:quant-qwen);
%      full per-task matrices live in appendix_quantisation_tables.tex
%      (tab:quant-te3s-full, tab:quant-qwen-full) --- \input that file from
%      the Appendix A section of main.tex or paste its tables there.
%      Numbers transcribed programmatically from the results JSONs.
%    - FILL-removal pass (author decision): no bootstrap intervals will be
%      attached to the matrix --- cells are stated as point estimates in the
%      opening paragraph and table notes, and the reading discipline (small
%      between-cell differences read qualitatively) is carried in the V-D
%      limitation. The Qwen native-dim sanity residue (NFCorpus 80.01 vs
%      80.02, Search 75.52 vs 75.54; TREC-CoVID exact) is reported as-is
%      with a NOTE at site on how to close it if ever challenged.
% ============================================================================

\section{Data and Results}
\label{sec:data-results}

This section reports the result of each component benchmark from the validation design in Section~\ref{subsec:evaluation-validation}, taking the components in turn. Findings are stated here as observed, and their interpretation against the research questions follows in Section~\ref{sec:analysis-discussion}.

% ----------------------------------------------------------------------------
\subsection{Keyword Generation}
\label{sec:results-keywords}

\begin{table}[t]
\caption{KPBiomed keyword extraction, present and absent F1@5 with stemming. Models are ordered by present F1, and GPT-5.4-nano is the deployed model.}
\label{tab:kw-kpbiomed}
\centering
\footnotesize
\begin{tabular}{@{}lrr@{}}
\toprule
Model & Present F1@5 & Absent F1@5 \\
\midrule
\textbf{GPT-5.4-nano} & \textbf{30.86} & 1.95 \\
MultipartiteRank & 19.58 & 0.00 \\
TF-IDF & 18.56 & 0.04 \\
KeyBART & 17.40 & \textbf{2.07} \\
YAKE & 16.79 & 0.02 \\
KBIR-inspec & 16.36 & 0.01 \\
KeyBERT-MiniLM & 8.52 & 0.04 \\
TextRank & 5.11 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

% [Fig. KPBiomed keyword F1@5 chart (fig:kw-kpbiomed) moved to Appendix A: Deferred Figures and Tables]

\begin{table}[t]
\caption{OAGKX keyword extraction, present and absent F1@5 with stemming. Models are ordered by present F1, and GPT-5.4-nano is the deployed model.}
\label{tab:kw-oagkx}
\centering
\footnotesize
\begin{tabular}{@{}lrr@{}}
\toprule
Model & Present F1@5 & Absent F1@5 \\
\midrule
KeyBART & \textbf{28.42} & \textbf{5.09} \\
\textbf{GPT-5.4-nano} & 26.49 & 1.77 \\
KBIR-inspec & 20.16 & 0.05 \\
MultipartiteRank & 18.83 & 0.00 \\
TF-IDF & 15.55 & 0.03 \\
KeyBERT-MiniLM & 12.04 & 0.04 \\
TextRank & 11.15 & 0.02 \\
YAKE & 8.85 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

% [Fig. OAGKX keyword F1@5 chart (fig:kw-oagkx) moved to Appendix A: Deferred Figures and Tables]

\begin{table}[t]
\caption{PubMedAKE keyword extraction, present and absent F1@5 with stemming. Models are ordered by present F1, and GPT-5.4-nano is the deployed model.}
\label{tab:kw-pubmedake}
\centering
\footnotesize
\begin{tabular}{@{}lrr@{}}
\toprule
Model & Present F1@5 & Absent F1@5 \\
\midrule
\textbf{GPT-5.4-nano} & \textbf{36.44} & \textbf{1.30} \\
TF-IDF & 24.06 & 0.03 \\
MultipartiteRank & 23.06 & 0.00 \\
KBIR-inspec & 19.36 & 0.01 \\
KeyBART & 18.41 & 1.29 \\
YAKE & 12.92 & 0.01 \\
KeyBERT-MiniLM & 6.78 & 0.01 \\
TextRank & 4.68 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

% [Fig. PubMedAKE keyword F1@5 chart (fig:kw-pubmedake) moved to Appendix A: Deferred Figures and Tables]

GPT-5.4-nano was benchmarked for keyword generation against the statistical and neural baselines of Section~\ref{subsec:evaluation-validation}, on KPBiomed, OAGKX, and PubMedAKE. Tables~\ref{tab:kw-kpbiomed} to~\ref{tab:kw-pubmedake} report the results for each dataset in turn. Throughout, present (extractive) keyphrases occur verbatim in the source text and absent (abstractive) keyphrases do not, and both are scored by F1@5 with light stemming.

On the present keyphrase tasks, GPT-5.4-nano records the highest score on KPBiomed and PubMedAKE and the second highest on OAGKX, where KeyBART leads. Its margin is widest on PubMedAKE, at 36.44 against a next-best of 24.06.

On the absent keyphrase tasks, which a model can only score on by producing keyphrases that do not occur in the source text, GPT-5.4-nano scores in the low single digits, from 1.30 on PubMedAKE to 1.95 on KPBiomed. These non-zero scores reflect the small generated share the prompt asks for, in that the model produces a minority of its keyphrases as higher-level terms rather than extracting all of them verbatim. The other generative baseline, KeyBART, behaves similarly and records the highest absent score on KPBiomed and OAGKX. The statistical extractors, TF-IDF, YAKE, TextRank, and MultipartiteRank, remain near zero, since they return only spans found in the source text and so do not generate at all.

\begin{table}[t]
\caption{Keyword extraction stability for GPT-5.4-nano over three runs on a 2000-document sample.}
\label{tab:kw-stability}
\centering
\footnotesize
\begin{tabular}{@{}p{0.66\columnwidth}r@{}}
\toprule
Quantity & Value \\
\midrule
Set-level Jaccard across runs & 0.73 \\
Present-F1 run-to-run std & $\pm$0.07 \\
Deterministic baselines (TF-IDF, YAKE, TextRank, MultipartiteRank, KeyBERT, KBIR) & 1.00 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:kw-stability} reports a reproducibility check for GPT-5.4-nano over three runs on a 2000-document sample. The set-level Jaccard similarity across runs is 0.73, so for a given document around 73 percent of the combined keyphrase set from any two runs is shared by both, equivalent to roughly one in six keyphrases changing between runs. The baselines are reproducible by construction. The run-to-run present-F1 standard deviation is $\pm$0.07, and the same sample reproduces the full-split figures to within roughly a tenth of a point, so the smaller stability sample does not shift the headline results.

% ----------------------------------------------------------------------------
\subsection{IR Extraction}

% [Table: Natural-language-to-IR accuracy (tab:ir-accuracy) moved to Appendix A: Deferred Figures and Tables]

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{pdfs/figures/data_and_results/ir_accuracy_ci.pdf}
\caption{Natural-language-to-IR joint accuracy by model on the hand-built gold set, with 95 percent bootstrap confidence intervals from 1000 resamples. Each model is shown in its as-deployed or best configuration. GPT-5.4-nano is a three-run mean (single-run range 0.931 to 0.940) and carries the percentile interval of a single run. Full per-model configuration and confidence bounds are given in Table~\ref{tab:ir-accuracy}.}
\label{fig:ir-accuracy}
\end{figure}

GPT-5.4-nano was benchmarked for natural-language-to-IR extraction against a set of hosted and local models, on the hand-built gold set of queries paired with their correct intermediate representations described in Section~\ref{subsec:evaluation-validation}. The headline measure, IR accuracy, is the joint figure reported in Table~\ref{tab:ir-accuracy}, counting a query as correct only when both its extracted entities and its Boolean expression are correct under exact matching.

Figure~\ref{fig:ir-accuracy} plots each model's accuracy with its 95 percent bootstrap interval. The two hosted models lead the benchmark by a clear margin, with GPT-5.4-nano highest at an IR accuracy of 0.937 and GPT-5-mini close behind at 0.931. Both hosted intervals clear those of every local model without overlap, placing the hosted advantage beyond sampling noise, while the two overlap each other, so this benchmark does not separate them.
% CHANGED (item 2, bootstrap audit): was "statistically indistinguishable".
% Overlapping marginal intervals do not test the difference; non-overlap
% establishes one, overlap is inconclusive. Wording aligned with the reading
% convention now stated in Section III-D. Among the local models, the two strongest (qwen3:8b and qwen3:4b) reach around 0.80, and the three smaller models fall away sharply to 0.207, 0.052, and 0.026, near the floor of the task.

% RESOLVED (author confirmed, July 2026 edit pass): the earlier stated range of
% 0.914 to 0.940 had absorbed the reasoning-OFF ablation run. Corrected to
% 0.931 to 0.940, the only range consistent with the 0.937 three-run mean, the
% 0.004 run-to-run std, and the majority-vote figures in tab:ir-stability
% (implying runs of {0.931, 0.940, 0.940}). Updated here and in the
% fig:ir-accuracy caption. NOT in this file: the tab:ir-accuracy table note in
% Appendix A carries the same 0.914 range and must be corrected there too.
Note, the headline 0.937 is the mean of three runs in the model's deployed, default-effort configuration, its single-run range spanning 0.931 to 0.940 (Table~\ref{tab:ir-stability}). Unless noted otherwise, IR-accuracy figures for this model refer to that configuration. Where a different configuration is reported below, in particular the reasoning ablation, its levels are not directly comparable to this headline.

\begin{table}[t]
\caption{Effect of reasoning on IR accuracy.}
\label{tab:ir-reasoning}
\centering
\footnotesize
\begin{tabular}{@{}lccc@{}}
\toprule
Model & Reasoning off & Reasoning on & $\Delta$ \\
\midrule
gpt-5.4-nano & 0.914 & 0.922 & +0.008 \\
gpt-5-mini & \textbf{0.750} & \textbf{0.940} & \textbf{+0.190} \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
Reasoning is controlled through the Azure \texttt{reasoning\_effort} setting with tokens accounted. Reasoning on used approximately 246 tokens per query for gpt-5.4-nano and 896 for gpt-5-mini, while reasoning off used none.
\end{minipage}
\end{table}

Table~\ref{tab:ir-reasoning} isolates the effect of reasoning on the two GPT models in a separate single-run ablation, one run per condition, so its levels are not directly comparable to the 0.937 and 0.931 headlines of Table~\ref{tab:ir-accuracy}. The quantity of interest is the within-model change from enabling reasoning. GPT-5.4-nano moves from 0.914 without reasoning to 0.922 with around 246 reasoning tokens per query, a difference of +0.008. GPT-5-mini moves from 0.750 without reasoning to 0.940 with around 896 reasoning tokens per query, a difference of +0.190. These two conditions bracket the default configurations of Table~\ref{tab:ir-accuracy}. The GPT-5 family's API default reasoning effort is medium, under which GPT-5-mini consumed around 384 reasoning tokens per query for its 0.931 headline, while GPT-5.4-nano's default configuration consumed none. The ablation's off condition therefore disables reasoning below the GPT-5-mini default, and its on condition spends a budget above it.
% VERIFY: state the exact reasoning_effort level used for the "reasoning on"
% condition (the ~896-token budget suggests high). Source for the medium
% default: OpenAI GPT-5 API documentation; Azure Foundry's reasoning docs list
% default exceptions only for gpt-5.1 (none) and gpt-5-pro (high), none for
% gpt-5-mini.

\begin{table}[t]
\caption{Deployed model, metric decomposition in the default-effort configuration.}
\label{tab:ir-decomp}
\centering
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
Metric & Value & 95\% CI \\
\midrule
IR accuracy (joint) & 0.937 & [0.897, 0.983] \\
Entity F1 (micro) & 0.984 & [0.969, 0.995] \\
Entity F1 (macro) & 0.982 & [0.967, 0.994] \\
Expression equivalence & 0.931 & [0.888, 0.974] \\
Expression graded & 0.985 & [0.974, 0.995] \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
Joint accuracy is bounded by expression equivalence, since entity extraction almost never fails on its own.
% ADDED (item 2, bootstrap audit): the joint point estimate is the three-run
% mean (0.9368) while the printed interval is the percentile bootstrap of the
% single-run headline (0.9397); no code path bootstraps a k-run mean. Audit
% option (b): keep 0.937 and disclose the pairing. The same disclosure is
% needed on the tab:ir-accuracy dagger note in Appendix A.
The joint point estimate is the mean of three runs and its interval is the percentile bootstrap of a single run whose headline is 0.940. The mismatch is immaterial, since run-to-run variation (a standard deviation of 0.004, Table~\ref{tab:ir-stability}) is an order of magnitude smaller than the interval width.
\end{minipage}
\end{table}

\begin{table}[t]
\caption{Deployed model, accuracy by stratum, ordered hardest first.}
\label{tab:ir-stratum}
\centering
\footnotesize
\begin{tabular}{@{}lrrr@{}}
\toprule
Stratum & $n$ & IR accuracy & failures \\
\midrule
\texttt{filter\_distractor} & 10 & 0.700 & 3 \\
\texttt{multi\_negation} & 10 & 0.800 & 2 \\
\texttt{deep\_nested} & 12 & 0.833 & 2 \\
\texttt{hard\_typing} & 12 & 0.917 & 1 \\
\texttt{single\_concept} & 6 & 1.000 & 0 \\
\texttt{multi\_concept} & 12 & 1.000 & 0 \\
\texttt{exclusion} & 12 & 1.000 & 0 \\
\texttt{nested} & 12 & 1.000 & 0 \\
\texttt{mixed\_entity} & 16 & 1.000 & 0 \\
\texttt{review\_style} & 14 & 1.000 & 0 \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
The count $n$ is small per stratum, at sixteen or fewer, so the failure counts are a more reliable read than the rate, and the per-stratum 95 percent intervals are wide, for example \texttt{filter\_distractor} at 0.700 is seven of ten.
\end{minipage}
\end{table}

Tables~\ref{tab:ir-decomp} and~\ref{tab:ir-stratum} decompose the deployed model's performance by component and by query type. By component, entity extraction records a micro F1 of 0.984 and a macro F1 of 0.982, where the micro figure pools every entity across all queries before scoring and the macro figure averages the per-query F1 scores, weighting every query equally. Expression equivalence is lower at 0.931, and a graded variant that credits partially correct expressions reaches 0.985. The joint IR accuracy of 0.937 is therefore bounded by the expression score, since entity extraction almost never fails on its own. By query type, the model records no errors on six of the ten strata, covering single and multiple concepts, exclusions, nesting, mixed entities, and review-style queries. Its errors concentrate in the remaining four. The largest source is \texttt{filter\_distractor}, where three of ten queries fail because the model miscategorises a filter term such as a year or citation bound as a searchable entity. The rest fall on the structurally hardest queries, with two of ten failing on multiple negations, two of twelve on deep nesting, and one of twelve on hard typing. Per-stratum counts are small, at sixteen queries or fewer, so the failure counts are a more reliable read than the rates.

\begin{table}[t]
\caption{Stability of the deployed model over three runs.}
\label{tab:ir-stability}
\centering
\footnotesize
\begin{tabular}{@{}p{0.46\columnwidth}cc@{}}
\toprule
Metric & temp 0 & temp 0.7 \\
\midrule
IR identical across all 3 runs & 0.966 & 0.957 \\
Run-to-run IR-accuracy std & 0.004 & 0.004 \\
Majority-vote@3 vs single run & 0.931 vs 0.940 & 0.940 vs 0.940 \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
Temperature zero is highly but not perfectly deterministic, and majority voting adds nothing.
\end{minipage}
\end{table}

Table~\ref{tab:ir-stability} reports a stability check over three runs. Although GPT-5.4-nano is not deterministic, at temperature zero it returns an identical IR on 96.6 percent of queries, with a run-to-run accuracy standard deviation of 0.004. Aggregating three runs by majority vote gives 0.931 against 0.940 for a single run, so the three-run mean of 0.937 reported in Table~\ref{tab:ir-accuracy} is used throughout.

% ----------------------------------------------------------------------------
\subsection{Dense Bi-encoder Retrieval}
\label{sec:results-biencoder}

% [Table: Dense bi-encoder full scores (tab:bienc-results) moved to Appendix A: Deferred Figures and Tables]

% Combined bi-encoder panel, spanning both columns at the top of the page.
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{pdfs/figures/data_and_results/biencoder_combined.pdf}
\caption{Dense bi-encoder nDCG across the three SciRepEval ad-hoc search tasks and their average, with 95 percent bootstrap confidence intervals from 1000 resamples. The selected model is shown in orange.}
\label{fig:bienc-avg}
\end{figure*}

The candidate bi-encoders were evaluated on the three ad-hoc search tasks of SciRepEval, namely NFCorpus, TREC-CoVID, and Search, scored by nDCG, and text-embedding-3-small was selected as the dense bi-encoder on the basis of this evaluation. The figure reported is SciRepEval full-ranking nDCG over a per-query candidate pool rather than BEIR full-corpus nDCG@10, which keeps the stage comparable to the scientific retrieval benchmark established in Section~\ref{subsec:evaluation-validation}. All scores are nDCG $\times$100 with 95 percent bootstrap intervals from 1000 resamples.

Table~\ref{tab:bienc-results} reports the per-task and average scores in full, and Figure~\ref{fig:bienc-avg} plots each model's average nDCG with its interval. The selected model records an average of 82.10. Qwen3-Embedding-4B holds the highest point estimate at 83.28, but the intervals of the top five models all overlap, forming a statistical tie that contains text-embedding-3-small along with EmbeddingGemma-300M, Qwen3-Embedding-0.6B, and mxbai-embed-large-v1. A clear margin separates this group from the lower three, SPECTER2-adhoc-query at 79.59, BGE-M3 at 79.36, and SPECTER2-base at 78.42, whose intervals fall below the leading cluster.

The average conceals a difference in where each model does its work, shown per task in Figure~\ref{fig:bienc-avg}. On NFCorpus and TREC-CoVID the general-purpose embedders lead, with Qwen3-Embedding-4B highest on both. On Search the order changes. SPECTER2-adhoc-query scores 78.22 against a field clustered near 75, and this is the single between-model gap on any one task whose interval clears the general models. The same model is weakest on NFCorpus at 70.46 and records the third-lowest average. The selected model sits within the leading group on each of the three tasks.

\begin{table}[t]
\caption{Deployment characteristics of the candidate bi-encoders.}
\label{tab:bienc-deploy}
\centering
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
Model & Params (approx.) & Embedding dim \\
\midrule
Qwen3-Embedding-4B & $\sim$4 B & 2560 \\
EmbeddingGemma-300M & $\sim$300 M & 768 \\
Qwen3-Embedding-0.6B & $\sim$0.6 B & 1024 \\
text-embedding-3-small & undisclosed & 1536 \\
mxbai-embed-large-v1 & $\sim$335 M & 1024 \\
SPECTER2-adhoc-query & $\sim$110 M & 768 \\
BGE-M3 & $\sim$568 M & 1024 \\
SPECTER2-base & $\sim$110 M & 768 \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
In-memory footprint per stored vector scales with embedding dimension at int8, at roughly one byte per dimension, giving around 115 GB at 768 dimensions, 154 GB at 1024, 230 GB at 1536, and 384 GB at 2560 over the 150 million works. Encoding cost scales with parameter count.
\end{minipage}
\end{table}

Table~\ref{tab:bienc-deploy} records the deployment characteristics that the nDCG scores do not, namely approximate parameter count and embedding dimension, with the int8 memory footprint each dimension implies over the 150 million works given in the table note. The selected model has an embedding dimension of 1536, placing its footprint at around 230 GB, and is served through a paid API rather than self-hosted.

% ----------------------------------------------------------------------------
\subsection{Cross-encoder Re-ranking}

% [Table: Cross-encoder full scores (tab:xenc-results) moved to Appendix A: Deferred Figures and Tables]

% Combined cross-encoder panel, spanning both columns at the top of the page.
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{pdfs/figures/data_and_results/crossencoder_combined.pdf}
\caption{Cross-encoder re-ranking nDCG across the three SciRepEval ad-hoc search tasks and their average, with 95 percent bootstrap confidence intervals from 1000 resamples. The selected model is shown in orange.}
\label{fig:xenc-avg}
\end{figure*}

The candidate rerankers were evaluated on the same three SciRepEval ad-hoc search tasks as the bi-encoder and under the same nDCG metric, scoring each model by the ranking it produces when it re-ranks the retrieved candidate pool, and Cohere Rerank 4.0 Pro was selected as the cross-encoder on the basis of this evaluation. All scores are nDCG $\times$100 with 95 percent bootstrap intervals from 1000 resamples. The intervals are tight on Search, which has 2585 queries, and wide on TREC-CoVID, which has 50.

Table~\ref{tab:xenc-results} reports the full per-task and average scores, and Figure~\ref{fig:xenc-avg} plots each model's average with its interval. The selected model holds the highest average at 83.47. Its interval overlaps those of the next three models, Qwen3-Reranker-4B at 83.13, Cohere Rerank 4.0 Fast at 82.78, and Qwen3-Reranker-0.6B at 82.43, so these four form a statistical tie at the top. Within that group the strongest self-hostable model, Qwen3-Reranker-4B, is tied with the selected model. Below the leading four the field descends steadily through a long tail to gte-reranker-modernbert-base at 65.95, a spread of more than seventeen points from top to bottom.

The per-task view in Figure~\ref{fig:xenc-avg} shows that almost all of this separation comes from NFCorpus, where the models spread across more than thirty points, from 80.42 at the top down to 48.88. On Search, by contrast, every reranker scores close to 75. TREC-CoVID falls between the two, with a high but noisy spread that reflects its fifty queries.

\begin{table}[t]
\caption{Deployment characteristics of the candidate cross-encoders.}
\label{tab:xenc-deploy}
\centering
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
Model & Params (approx.) & Licence \\
\midrule
Cohere Rerank 4.0 Pro & undisclosed & Paid API \\
Qwen3-Reranker-4B & $\sim$4 B & Apache-2.0 \\
Cohere Rerank 4.0 Fast & undisclosed & Paid API \\
Qwen3-Reranker-0.6B & $\sim$0.6 B & Apache-2.0 \\
mxbai-rerank-large-v1 & $\sim$435 M & Open \\
jina-reranker-v2-base & $\sim$278 M & CC-BY-NC \\
MedCPT-Cross-Encoder & $\sim$109 M & Open \\
ms-marco-MiniLM-L-6-v2 & $\sim$22 M & Open \\
bge-reranker-v2-m3 & $\sim$568 M & Open \\
ms-marco-MiniLM-L-12-v2 & $\sim$33 M & Open \\
bge-reranker-large & $\sim$560 M & Open \\
bge-reranker-base & $\sim$278 M & Open \\
gte-reranker-modernbert-base & $\sim$150 M & Open \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
A reranker stores no vectors, so its cost is query-time latency, which scales with parameter count, rather than stored memory. Licences marked ``Open'' are self-hostable but their exact terms should be confirmed against the deployment before selection. The jina-reranker-v2 weights are CC-BY-NC-4.0 and so are non-commercial and not deployable in a commercial product.
\end{minipage}
\end{table}

Table~\ref{tab:xenc-deploy} records the deployment characteristics, namely approximate parameter count and licence. Unlike the bi-encoder, a reranker stores no vectors, so its per-query cost is latency, which scales with parameter count, rather than stored memory. The self-hostable options carry mixed licences, with the Qwen3 rerankers released under Apache-2.0 and the jina reranker under a non-commercial CC-BY-NC licence, while the selected model is served through a paid API.

% ----------------------------------------------------------------------------
\subsection{Embedding Quantisation}
\label{sec:results-quant}
% RESOLVED (item 8): placeholder replaced with the dimension x precision
% matrix results. Reporting register only; interpretation (noise reading,
% frontier reading, operating points) lives in Section V per the
% de-duplication convention. Full per-task matrices: see the appendix
% snippet file appendix_quantisation_tables.tex.

The dimension-by-precision matrix of Section~\ref{subsubsec:embedding-quantisation} was run for the deployed bi-encoder, text-embedding-3-small, and the strongest self-hosted candidate by point estimate, Qwen3-Embedding-4B. Every configuration is derived from the cached native-dimension fp16 embeddings of the bi-encoder evaluation, without re-encoding the corpus, and scored through the same pipeline, so the native-dimension fp16 row of each matrix is the sanity condition against Table~\ref{tab:bienc-results}. For text-embedding-3-small the condition holds exactly on all three tasks. For Qwen3-Embedding-4B it holds exactly on TREC-CoVID, while NFCorpus and Search agree to within 0.02, well below the smallest between-configuration difference the matrix is read for.
% NOTE (author decision): the Qwen residue is reported as-is rather than
% regenerated. If it is ever challenged, re-deriving the matrix from the
% exact embedding cache behind Table XV should close it; the likely cause is
% the matrix reading a separate export of the same embeddings.
Tables~\ref{tab:quant-te3s} and~\ref{tab:quant-qwen} report the average nDCG of every configuration, its retention of the native fp16 baseline, and the projected int8 index size over the 150 million works. The full per-task matrices are given in Tables~\ref{tab:quant-te3s-full} and~\ref{tab:quant-qwen-full}. All scores are nDCG $\times$100 and are reported as point estimates, with their reading qualified in Section~\ref{subsec:limitations}.

\begin{table}[t]
\caption{Dimension $\times$ precision matrix for the deployed bi-encoder, text-embedding-3-small. Average nDCG $\times$100 over the three SciRepEval ad-hoc search tasks, retention against the native 1536-dimension fp16 baseline, and projected int8 index size over the 150 million works.}
\label{tab:quant-te3s}
\centering
\footnotesize
\begin{tabular}{@{}rccccr@{}}
\toprule
Dim & fp16 & int8 & fp8 & Retention & GB (int8) \\
\midrule
1536 & 82.10 & 82.11 & 82.09 & 100.0\% & 231.0 \\
1024 & 81.99 & 81.99 & 81.99 & 99.9\%  & 154.2 \\
768  & 81.92 & 81.91 & 81.92 & 99.8\%  & 115.8 \\
512  & 81.61 & 81.61 & 81.62 & 99.4\%  & 77.4  \\
384  & 81.44 & 81.46 & 81.43 & 99.2\%  & 58.2  \\
256  & 80.94 & 80.93 & 80.91 & 98.6\%  & 39.0  \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
Each configuration is derived from the cached native-dimension fp16 embeddings, without re-encoding, by truncation to the target dimension, L2 re-normalisation, and quantisation, and scored through the same pipeline as Table~\ref{tab:bienc-results}. Retention is the int8 average as a share of the native fp16 average. Storage assumes one byte per dimension plus a four-byte scale per vector at int8. All cells are point estimates.
\end{minipage}
\end{table}

\begin{table}[t]
\caption{Dimension $\times$ precision matrix for the strongest self-hosted candidate, Qwen3-Embedding-4B. Average nDCG $\times$100 over the three SciRepEval ad-hoc search tasks, retention against the native 2560-dimension fp16 baseline, and projected int8 index size over the 150 million works.}
\label{tab:quant-qwen}
\centering
\footnotesize
\begin{tabular}{@{}rccccr@{}}
\toprule
Dim & fp16 & int8 & fp8 & Retention & GB (int8) \\
\midrule
2560 & 83.27 & 83.27 & 83.26 & 100.0\% & 384.6 \\
1536 & 83.26 & 83.24 & 83.25 & 100.0\% & 231.0 \\
1024 & 83.17 & 83.18 & 83.16 & 99.9\%  & 154.2 \\
768  & 82.99 & 83.00 & 83.02 & 99.7\%  & 115.8 \\
512  & 82.80 & 82.77 & 82.82 & 99.4\%  & 77.4  \\
384  & 82.45 & 82.45 & 82.43 & 99.0\%  & 58.2  \\
256  & 82.09 & 82.09 & 82.11 & 98.6\%  & 39.0  \\
\bottomrule
\end{tabular}

\vspace{3pt}
\begin{minipage}{\linewidth}
\footnotesize\itshape
Derivation, retention, and storage as in Table~\ref{tab:quant-te3s}.
\end{minipage}
\end{table}

On the precision axis, fp16, int8, and fp8 agree to within 0.15 nDCG on any single task and 0.06 on the three-task average at every dimension of both models, and the sign of the difference varies by cell. At the native dimensions the deployed model scores 82.11 at int8 against 82.10 at fp16, and Qwen3-Embedding-4B scores 83.27 at both. fp8 tracks int8 throughout while saving four bytes per vector, around 0.6\,GB over the corpus.

On the dimension axis, the averages fall monotonically but slowly. The deployed model retains 99.9 percent of its baseline at 1024 dimensions, 99.8 percent at 768, 99.4 percent at 512, and 98.6 percent at 256. Qwen3-Embedding-4B is flat from 2560 to 1536 dimensions (83.26 against 83.27), then retains 99.9 percent at 1024, 99.7 percent at 768, and 98.6 percent at 256. Per task, the fall concentrates on NFCorpus, which loses roughly 2.3 points from the native dimension to 256 for both models, while TREC-CoVID and Search each move by less than a point over the same range (Tables~\ref{tab:quant-te3s-full} and~\ref{tab:quant-qwen-full}).

The projected index size scales with both axes, so configurations can be compared at matched storage. At every matched budget the higher-dimension configuration at lower precision scores higher than the lower-dimension one at fp16. At around 115\,GB over the corpus, 768 dimensions at int8 scores 81.91 against 81.44 for 384 dimensions at fp16 for the deployed model, and 83.00 against 82.45 for Qwen3-Embedding-4B. Across models at int8, Qwen3-Embedding-4B truncated to 768 dimensions scores 83.00 in 115.8\,GB, against 82.11 for the deployed model at its native 1536 dimensions in 231.0\,GB.

% ----------------------------------------------------------------------------
\subsection{System-Level Evaluation}
\label{sec:results-system}
% Placeholder. Results not yet available.
% TODO: insert the per-query characterisations and light relevance judgments
% here once the evaluation runs; the design is specified in Section III-D and
% the analysis framework lives in Section V (subsec:rq3).
% Comparator pass (July 2026): comparators updated to the three engines;
% the RAG-LLM comparator removed (see research_design III-D6).
The system-level evaluation specified in Section~\ref{subsec:evaluation-validation} compares the assembled cascade against Google Scholar, PubMed, and Semantic Scholar over a curated set of realistic queries. Its per-query characterisations and light relevance judgments are reported in the final submission and analysed against RQ3 in Section~\ref{subsec:rq3}.